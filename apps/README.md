# Apps directory

Place Windows XP-era `.exe` files (and any required companion DLLs) in this directory.

When you launch `xp-layer`, the graphical app picker will list every `.exe` found here.
Selecting an application and clicking **Run** will start the translation layer for that binary.

This workflow is intentionally similar to touchHLE’s `touchHLE_apps` directory.
