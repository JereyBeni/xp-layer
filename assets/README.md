# Assets

Place the application logo here as **`logo.png`**.

The program will automatically use `assets/logo.png` as the window / taskbar icon when the file is present.

Recommended: square PNG (for example 256x256 or 512x512).
